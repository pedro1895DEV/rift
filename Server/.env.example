# Backend RIFT — Variáveis de Ambiente

# Porta do servidor (padrão: 3000)
PORT=3000

# URL do webhook N8N para IA (padrão: http://localhost:5678/webhook/rift-ai)
N8N_WEBHOOK_URL=http://localhost:5678/webhook/rift-ai

# Frontend URL (CORS) — configurado em index.ts
# FRONTEND_URL=http://localhost:5173
